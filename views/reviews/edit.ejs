<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/style.css" />
    <title>Edit Review Page</title>
  </head>
  <body>
    <%- include('../partials/_navbar.ejs') %>
    <main class="container page-form">
    <h1>Edit Review</h1>
    <form
      action="/games/<%= game._id %>/reviews/<%= review._id %>?_method=PUT"
      method="POST"
    >
      <label>
        Rating (0-5):
        <input
          type="number"
          name="rating"
          min="0"
          max="5"
          step="0.1"
          required
          value="<%= review.rating %>"
        />
      </label>

      <label>
        Comment:
        <textarea name="comment" required><%= review.comment %></textarea>
      </label>

      <button type="submit">Update Review</button>
    </form>
      <div class="form-actions">
          <a class="btn" href="/games/<%= game._id %>">Cancel</a>
      </div>
    </main>
  </body>
</html>
